import { Link } from "react-router-dom";

function Header() {
    return (
        <header>
            <Link to="/">Fast React Pizza Company</Link>

            <p>Niki</p>
        </header>
    );
}

export default Header;
